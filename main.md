# Traceback Backend Guide

이 저장소는 TRACEBACK 커머스 백엔드를 구현한다. 실제 구현을 시작할 때는
[`docs/simple-build-guide.md`](./docs/simple-build-guide.md)를 먼저 보고, 기존 상세 문서는 참고 자료로 사용한다.

## 작업 시작

새 세션은 이전 대화나 기억을 전제로 하지 않는다.

1. `docs/simple-build-guide.md`를 읽고 현재 Phase의 구현 원칙을 확인한다.
2. `docs/build-plan.md`의 "재개 체크포인트"를 읽는다.
3. 체크포인트에 기록된 다음 행동과 관련 상세 문서 섹션만 참고한다.
4. 현재 작업 슬라이스 범위 안에서 모델, API, 테스트를 함께 구현한다.
5. 작업 중 중요한 판단과 검증 결과를 즉시 체크포인트에 기록한다.
6. 작업을 멈추기 전 체크포인트가 현재 코드 상태와 일치하는지 확인한다.

사용자가 새 세션에서 "`main.md`로 시작해줘"라고 요청하면 위 절차를 수행한다. 체크포인트에 막힌 결정이 없으면 추가 질문 없이 정확한 다음 행동부터 진행한다.

| 작업 | 기준 문서 |
| --- | --- |
| 간단 구현 기준, 확정/질문 원칙 | [`docs/simple-build-guide.md`](./docs/simple-build-guide.md) |
| 구현 순서, 현재 범위, 완료 조건 | [`docs/build-plan.md`](./docs/build-plan.md) |
| 모델 후보, 필드 후보, 관계/제약 아이디어 | [`docs/domain-model.md`](./docs/domain-model.md) |
| 상태 전이, 트랜잭션, 동시성, 재고 흐름 후보 | [`docs/order-flow.md`](./docs/order-flow.md) |
| 엔드포인트, 요청/응답, 외부 계약 후보 | [`docs/api-spec.md`](./docs/api-spec.md) |

## 변경 규칙

- 상세 문서는 설계 후보와 결정 배경이며, 현재 구현 범위는 `simple-build-guide.md`와 `build-plan.md`가 함께 정한다.
- 코드와 테스트 결과는 설계를 검증하는 증거이지 자동으로 새 명세가 되지는 않는다.
- 구현 중 설계 문제가 발견되면 `build-plan.md`의 설계 변경 루프를 따른다.
- 결정된 변경은 필요한 문서, Phase 범위, 코드, 테스트에 함께 반영한다.
- 영향이 없는 문서는 수정하지 않는다.
- 긴급하게 코드가 먼저 바뀌어도 작업을 닫기 전에 문서와 회귀 테스트를 동기화한다.
- 설계 문제가 의심되면 임시 우회 구현을 완료 처리하지 않는다. 원인과 선택지를 체크포인트에 기록하고, 결정 후 필요한 문서만 수정한다.

PR 피드백 처리:

- 구현 방식, 네이밍, 테스트 누락 같은 피드백은 코드/테스트만 수정한다.
- API 계약, 모델/제약, 상태 흐름, 현재 범위를 바꾸는 피드백은 설계 변경으로 분류한다.
- 설계 변경 피드백은 영향받는 문서를 코드/테스트와 같은 PR에서 수정한다.
- PR 피드백 반영 후 재개 체크포인트의 완료 지점, 미해결 사항, 다음 행동을 갱신한다.

## 작업과 PR 크기

- 한 작업 슬라이스와 한 PR의 변경 파일은 최대 30개다. 추가/수정/삭제 파일을 모두 포함한다.
- 생성 파일과 migration도 변경 파일 수에 포함한다.
- 30개를 초과할 가능성이 있으면 구현 전에 독립 검증 가능한 슬라이스로 나눈다.
- 파일 수를 맞추기 위해 서로 무관한 변경을 합치거나, 필요한 테스트/문서 변경을 생략하지 않는다.
- 공통 기반 변경이 여러 후속 작업에 필요하면 기반 PR을 먼저 분리한다.

## 브랜치 전략

- 최초 Docker/Compose 부트스트랩만 `main`에서 직접 작업한다.
- Docker 부트스트랩 검증과 커밋이 끝난 시점의 `main`에서 `development` 브랜치를 생성한다.
- 이후 모든 작업 브랜치는 `development`에서 분기하고 PR 대상도 `development`로 한다.
- 배포 가능한 단위가 검증되면 `development`를 `main`으로 반영한다.
- `main`과 `development`에는 긴급 수정 외의 기능 작업을 직접 진행하지 않는다.
- 브랜치 전환이나 생성 전에는 재개 체크포인트와 작업 트리가 현재 상태를 정확히 반영하는지 확인한다.

## 문서 동기화

| 변경 내용 | 함께 확인할 문서 |
| --- | --- |
| 필드, 관계, unique/check 제약 | `domain-model.md` + 영향받는 API/흐름 |
| 상태 전이, 트랜잭션, 재고, 멱등성 | `order-flow.md` + `domain-model.md` + `api-spec.md` |
| 요청/응답, 인증, 오류, 상태 enum | `api-spec.md` + 영향받는 모델/흐름 |
| 구현 범위, 순서, 완료 조건 | `build-plan.md` |

## 공통 불변 원칙

- 주문/결제/출고 상태는 원천 데이터에서 계산하고 중복 저장하지 않는다.
- 외부 PG 호출은 DB 트랜잭션 밖에서 수행한다.
- 재고 경쟁은 DB 제약, row lock, 결정적 idempotency key로 보호한다.
- 비회원 주문 접근 토큰과 결제 confirm 토큰은 분리하고 원문을 저장하지 않는다.
- 결과가 불명확한 결제·환불·재고 조정 중에는 출고를 차단한다.
- Phase 9+ 기능은 구현 전까지 API에서 활성화하지 않는다.

## MVP 완료

Phase 0~8의 완료 조건과 단계 진행 체크리스트가 모두 통과하고, 실제 API가 해당 Phase에서 확정한 계약과 일치하면 백엔드 MVP 완료로 본다. Phase 9+ 확장 기능은 MVP 완료를 막지 않는다.
