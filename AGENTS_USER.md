# TRACEBACK 작업 규칙 사용자용 요약

이 문서는 `AGENTS.md`의 사용자 읽기용 한국어 요약이다.
AI 실행 지침 전체를 그대로 옮기는 문서가 아니라, 이 저장소에서 작업이 어떻게 진행되는지
사용자가 빠르게 확인하기 위한 기준이다.

## 시작 기준

- 새 작업은 먼저 `docs/simple-build-guide.md`의 범위와 원칙을 기준으로 본다.
- 이어서 `docs/build-plan.md`의 재개 체크포인트를 확인한다.
- 현재 작업에 필요한 상세 문서만 추가로 참고한다.
- 작업 전 현재 브랜치와 작업트리를 확인한다.
- 대화 맥락만으로 Phase가 완료됐다고 가정하지 않는다.

## Phase 진행

- Phase 범위의 기준 문서는 `docs/simple-build-guide.md`와 `docs/build-plan.md`다.
- Phase 0 완료 기준이 실제 코드, 테스트, 체크포인트에 반영되기 전에는 Phase 1 Catalog를 시작하지 않는다.
- 개발을 마치거나 멈출 때는 `docs/current/README.md`와 `docs/build-plan.md`의 현재 상태를 실제 코드 상태와 맞춘다.
- 완료 이력은 검증이 끝난 뒤에만 기록한다.

## 브랜치와 변경 범위

- 작업 브랜치는 `development`에서 만든다.
- `main`이나 `development`에서 직접 기능 개발하지 않는다.
- 기존 사용자 변경은 보존한다.
- 한 슬라이스는 PR 하나로 검증 가능한 크기로 유지한다.
- 기본 변경 파일 수 제한은 30개다.
- 구현과 맞지 않는 임시 계획 문구는 문서에 남기지 않는다.

## 사용자 확인이 필요한 경우

다음 네 가지는 사용자가 직접 확인한다.

- PR 리뷰 대응
- 비즈니스 또는 제품 선택사항
- 기술 스택, 아키텍처, 인프라 같은 기술 선택
- 사용자가 직접 처리해야 하는 외부 연동

그 외 구현 세부사항은 중간 확인 없이 계속 진행하고,
구현, 검증, 문서 갱신까지 마친 뒤 결과를 보고한다.

예를 들면 다음은 사용자 확인 대상이다.

- 모델이나 API가 브랜드 운영 방식, 고객 경험, 관리자 정책을 결정하는 경우
- 재고, 결제, 환불, 취소 방식이 운영 리스크나 고객 응대 정책을 바꾸는 경우
- OAuth, PG, 이메일/SMS 발송처럼 외부 서비스 설정이 필요한 경우
- 프레임워크, 라이브러리, 인증 방식, 배포 방식 같은 기술 선택이 필요한 경우

반대로 이미 문서에 방향이 정해져 있고 위험이 낮은 구현 세부사항은 작업자가 판단해 진행한다.

## 프론트엔드 작업

- 프론트엔드 구현은 `../traceback-client` 저장소에서 한다.
- 프론트 작업 전 `../traceback-client/AGENTS.md`를 읽는다.
- 백엔드와 프론트가 함께 바뀌는 작업은 두 저장소의 브랜치와 작업트리를 모두 확인한다.
- 사용자가 명시하지 않는 한 백엔드 저장소 안에 프론트 파일을 만들지 않는다.

## 모델 문서화

- 모델은 핵심 구현 산출물로 취급한다.
- 모델 작업 후에는 모델과 컬럼 목적을 읽기 쉬운 문서로 남긴다.
- MVP에 필요 없는 컬럼은 미리 추가하지 않는다.
- 실제 migration과 모델 목적 문서는 서로 맞아야 한다.

## 문서 동기화

- `AGENTS.md`가 작업 방식, 확인 기준, 구현 방식, 검증, 보고 방식에 영향을 주도록 바뀌면 이 문서도 함께 갱신한다.
- 이 문서는 사용자용 요약이므로 AI용 세부 프롬프트 문구를 그대로 복사하지 않는다.
- 사용자가 새 세션에서 작업 방식만 빠르게 이해할 수 있게 실무 기준 중심으로 유지한다.
